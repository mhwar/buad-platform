# Cloudflare Pages serves clean URLs automatically:
#   index.html  → /        (public landing page)
#   app.html    → /app      (dashboard, team only; internal routing is hash-based)
# No explicit rules needed. A /app → /app.html rewrite here conflicts with the
# automatic .html-stripping and breaks /app, so it is intentionally omitted.
